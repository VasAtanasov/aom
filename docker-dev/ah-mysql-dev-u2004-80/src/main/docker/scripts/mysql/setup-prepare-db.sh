#!/bin/bash -eu

# Place holder of anything that must be done after the server have started but the container main process is not yet blocked.
# Extending images may replace the script with code that need to executed - dump imports or other